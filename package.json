{
  "name": "gamesite3-deepstream",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "coverage": "jest --coverage --collectCoverageFrom=\"src/**.js\"",
    "jest": "jest",
    "prettier": "prettier --write --single-quote --trailing-comma es5 *.js src/**.js",
    "test": "prettier --list-different --single-quote --trailing-comma es5 *.js src/**.js && npm run coverage"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "prettier": "^1.3.1"
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "cookie": "^0.3.1",
    "deepstream.io": "^2.2.2",
    "dotenv": "^4.0.0",
    "jsonwebtoken": "^7.4.1"
  }
}
