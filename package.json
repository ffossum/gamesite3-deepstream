{
  "name": "gamesite3-deepstream",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "jest": "jest",
    "prettier": "prettier --write --single-quote --trailing-comma es5 *.js src/**/*.js",
    "test": "prettier --list-different --single-quote --trailing-comma es5 *.js src/**/*.js && npm run jest"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "prettier": "^1.3.1"
  },
  "dependencies": {
    "cookie": "^0.3.1",
    "deepstream.io": "^2.2.2",
    "jsonwebtoken": "^7.4.1"
  }
}
